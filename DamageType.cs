// DamageType.cs (YENÝ SCRIPT)

// Bu bir bileþen (component) olmadýðý için MonoBehaviour'a ihtiyaç duymaz.
// Projedeki herhangi bir yerden eriþilebilen basit bir veri türüdür.
public enum DamageType { Physical, Magical, True }